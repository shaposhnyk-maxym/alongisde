{
  "name": "alongside-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Symmetric diary-unlock push trigger (see docs/backend-plan.md and docs/roadmap.md M17).",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "jest",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-alongside \"jest --config jest.emulator.config.js\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "13.10.0",
    "firebase-functions": "7.2.5"
  },
  "devDependencies": {
    "@types/jest": "30.0.0",
    "@types/node": "20.19.43",
    "firebase-tools": "15.24.0",
    "jest": "30.4.2",
    "ts-jest": "29.4.11",
    "typescript": "5.9.3"
  }
}
